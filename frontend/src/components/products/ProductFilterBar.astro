---
import ProductSearchBar from './ProductSearchBar.astro';

interface Props {
  locale: 'en' | 'ko';
}

const { locale } = Astro.props;
---

<div class="product-filter-bar">
  <ProductSearchBar locale={locale} />
</div>

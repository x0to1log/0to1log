---
export const prerender = false;
const acceptLang = Astro.request.headers.get('accept-language') || '';
const locale = acceptLang.split(',').some(l => l.trim().startsWith('ko')) ? 'ko' : 'en';
return Astro.redirect(`/${locale}/`, 307);
---
